function testJS(){
    return "HERE I AM JS";
}

/*function generate_graph(arg0, arg1){
console.log(arg0 + " :: " + arg1);
                $.jqplot.config.enablePlugins = true;
               
                var arr = data;

                for (i = 0; i < arr.length; i++) {
                    entry = arr[i];
		    console.log("///NEW ARR///");
                    for (j = 0; j < entry.length; j++) {
                        console.log( entry[j] );
                    }
                }

		plot1 = $.jqplot ('chart1', data, {
         
          highlighter: {
             sizeAdjust: 14,
             tooltipLocation: 'n',
             tooltipAxes: 'y',
             formatString:'#serieLabel# - %s',
             useAxesFormatters: false
         },
	 axes: {
            xaxis: {
                label: 'Resets per Test Run ',
		tickInterval: 1
                     } 
        },
	 grid: {
            backgroundColor: '#EBEBEB',
            borderWidth: 0,
            gridLineColor: 'grey',
            gridLineWidth: 1,
            borderColor: 'black'
         },
	 legend: {
            show: true,
            placement: 'outside'
         },
         cursor: {
             show: true,
             zoom: true

         },
	 series:[ series ], 
      });

}*///end generate_graph()
